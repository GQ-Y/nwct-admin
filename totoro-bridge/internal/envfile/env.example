# totoro-bridge 默认环境变量模板（会在首次运行时自动写入到二进制同目录的 .env）
#
# 说明：
# - 直接运行 totoro-bridge 即可（默认监听 :18090，默认 db ./bridge.db）
# - 如需修改配置，编辑同目录 .env 或设置系统环境变量
# - 重要：生产环境请务必设置 TOTOTO_BRIDGE_ADMIN_KEY（否则管理接口无鉴权）

# 监听地址
TOTOTO_BRIDGE_ADDR=:18090

# 数据库路径
TOTOTO_BRIDGE_DB=./bridge.db

# 管理后台鉴权 key（可留空表示不校验；生产建议设置）
TOTOTO_BRIDGE_ADMIN_KEY=

# ticket 有效期（天），默认 30
TOTOTO_TICKET_TTL_DAYS=30


